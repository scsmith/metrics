node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# macOS
.DS_Store

# ignore the docker-compose file as it just creates node for local dev
/docker-compose.yml

# ignore local vscode setup for now
.devcontainer/

# Don't commit the .env file, we will probably commit an example one in future
.env

# We're using yarn so don't add a package-lock in case it's generated by mistake
package-lock.json
